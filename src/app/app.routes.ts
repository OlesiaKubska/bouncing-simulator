import { Routes } from '@angular/router';
import { BouncySimulatorComponent } from './bouncy-simulator/bouncy-simulator.component';

export const routes: Routes = [
  { path: '', component: BouncySimulatorComponent },
];
